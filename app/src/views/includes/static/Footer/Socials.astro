---
export interface Props {
  sx?: string;
}
import Socials from "@components/static/Socials/index.astro";
export const defaultProps = {};

const { sx } = Astro.props;
const socials = [];
---

<div class="flex flex-wrap -m-1.5">
  {socials.map((social) => <Socials />) ?? <Socials />}
</div>
