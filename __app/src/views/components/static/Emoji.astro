---
import Emoji from "astro-emoji";

export interface Props {
  symbol?: string;
  label?: string;
}

const { symbol, label } = Astro.props;
---

<Emoji symbol={symbol ?? "💗"} label={label ?? "love"} />
